<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to public/ (Hostinger: document root = project root)
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
